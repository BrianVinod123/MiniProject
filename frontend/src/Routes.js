import { BrowserRouter as Routes,Route,Switch, BrowserRouter, Router } from "react-router-dom";

const Routes=()=>{
    <BrowserRouter>
        <Router>
            <Routes>
                <Route path="/">
                </Route>
            </Routes>
        </Router>
    </BrowserRouter>
}

export default Routes